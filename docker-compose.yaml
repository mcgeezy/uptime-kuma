---
services:
  uptime-kuma:
    build:
       context: .
       dockerfile: Dockerfile
    image: mcgeezy-kuma:latest
    container_name: uptime-kuma
    restart: always
    ports:
      - "3001:3001"
    volumes:
      - ./data:/app/data
